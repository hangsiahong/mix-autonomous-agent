[
  {
    "name": "web_search",
    "description": "Search the web. Auto-selects backend: Tavily (if TAVILY_API_KEY set) \u2192 Exa \u2192 Brave \u2192 SearXNG \u2192 DuckDuckGo. Returns titles, URLs, and snippets. SSRF-protected.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query string."
        },
        "limit": {
          "type": "integer",
          "description": "Number of results to return (1-15, default 8)."
        }
      },
      "required": [
        "query"
      ]
    },
    "toolset": "search"
  },
  {
    "name": "fetch_url",
    "description": "Fetch the content of a URL as clean Markdown (via Jina Reader). SSRF-protected \u2014 blocks private IPs and cloud metadata endpoints. Optionally uses LLM to extract the relevant portion when 'query' is provided. Truncates large pages intelligently around matching content.",
    "parameters": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "The full URL to fetch (must be a public http/https URL)."
        },
        "query": {
          "type": "string",
          "description": "Optional: what you want to extract from the page. If provided, content is centered around matching sections and optionally summarized by LLM."
        },
        "max_chars": {
          "type": "integer",
          "description": "Max characters to return before truncation/summarization (default 8000)."
        }
      },
      "required": [
        "url"
      ]
    },
    "toolset": "search"
  },
  {
    "name": "bash",
    "description": "Run a bash command in the project directory. Use this to read files (cat), list directories (ls), search code (grep), check git status, run scripts, or any shell operation. SAFETY: blocks reads of /etc/{passwd,shadow}, ~/.ssh, .env, credential exfil patterns, reverse shells, and obviously destructive commands (rm -rf /, mkfs, dd of=/dev/sdX, fork bombs). Hard-capped at TOOL_timeout seconds (default 30, max 120).",
    "parameters": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The bash command to run."
        },
        "timeout": {
          "type": "integer",
          "description": "Seconds before the command is killed. Default 30, max 120."
        }
      },
      "required": [
        "command"
      ]
    },
    "toolset": "core"
  },
  {
    "name": "write_file",
    "description": "Create or overwrite a file. Use for creating new files from scratch \u2014 prefer edit_code or patch when modifying existing files. For .sh / .py / .json the new content is syntax-checked before any write. Refuses sensitive system paths and credential directories. Set append=true to add to an existing file.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Relative path to the file to create or overwrite."
        },
        "content": {
          "type": "string",
          "description": "The full content to write to the file."
        },
        "append": {
          "type": "boolean",
          "description": "If true, append to the file instead of overwriting. Default: false."
        }
      },
      "required": [
        "path",
        "content"
      ]
    },
    "toolset": "core"
  },
  {
    "name": "search_files",
    "description": "Search for a text pattern across project files using grep. Returns matching lines with file paths and line numbers.",
    "parameters": {
      "type": "object",
      "properties": {
        "pattern": {
          "type": "string",
          "description": "The text or regex pattern to search for."
        },
        "path": {
          "type": "string",
          "description": "Directory or file to search in. Defaults to project root."
        },
        "file_glob": {
          "type": "string",
          "description": "Filename glob to filter files, e.g. '*.sh' or '*.py'. Default: all files."
        },
        "ignore_case": {
          "type": "boolean",
          "description": "Case-insensitive search. Default: false."
        }
      },
      "required": [
        "pattern"
      ]
    },
    "toolset": "search"
  },
  {
    "name": "todo",
    "description": "Manage a per-session task list. Use to plan and track multi-step work. Actions: add, list, done, delete, clear.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "add",
            "list",
            "done",
            "delete",
            "clear"
          ],
          "description": "Action to perform."
        },
        "session": {
          "type": "string",
          "description": "Session identifier for the todo list. Defaults to 'default'."
        },
        "text": {
          "type": "string",
          "description": "Task description (required for 'add')."
        },
        "id": {
          "type": "string",
          "description": "Task ID (required for 'done' and 'delete')."
        }
      },
      "required": [
        "action"
      ]
    },
    "toolset": "meta"
  },
  {
    "name": "clarify",
    "description": "Send a clarifying question to the user before taking an ambiguous or irreversible action. Stops the current turn and waits for their reply.",
    "parameters": {
      "type": "object",
      "properties": {
        "question": {
          "type": "string",
          "description": "The question to ask the user."
        }
      },
      "required": [
        "question"
      ]
    },
    "toolset": "core"
  },
  {
    "name": "process",
    "description": "Run shell commands in the background and manage them. Actions: run (start), list (show all), output (get logs), kill (stop).",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "run",
            "list",
            "output",
            "kill"
          ],
          "description": "Action to perform."
        },
        "command": {
          "type": "string",
          "description": "The bash command to run in background (required for 'run')."
        },
        "name": {
          "type": "string",
          "description": "A label for the process. Used to reference it in output/kill. Auto-generated if not provided."
        }
      },
      "required": [
        "action"
      ]
    },
    "toolset": "meta"
  },
  {
    "name": "session_search",
    "description": "Search past conversation sessions for a topic or keyword. Ranks sessions by relevance, extracts context windows around matches, and uses LLM to summarize the top matching sessions. Perfect for recalling past conversations, decisions, or code from earlier chats.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The topic, keyword, or phrase to search for across past sessions."
        },
        "session": {
          "type": "string",
          "description": "Specific session ID to search (e.g. 'tg_670967877'). Searches all sessions if omitted."
        },
        "limit": {
          "type": "integer",
          "description": "Max number of sessions to return (1-5). Default: 3."
        }
      },
      "required": [
        "query"
      ]
    },
    "toolset": "memory"
  },
  {
    "name": "memory",
    "description": "Manage persistent curated memory across two stores: 'memory' (your own notes, facts, learnings) and 'user' (what you know about the user). These are injected into every session as a frozen snapshot. Use add/replace/remove to maintain them. Do NOT store sensitive data, credentials, or large blobs.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "add",
            "replace",
            "remove",
            "read"
          ],
          "description": "add: append a new fact. replace: find old_text and swap it. remove: delete entry containing old_text. read: show all entries."
        },
        "target": {
          "type": "string",
          "enum": [
            "memory",
            "user"
          ],
          "description": "Which store: 'memory' for agent notes (3000 char limit), 'user' for user profile (2000 char limit). Defaults to 'memory'."
        },
        "content": {
          "type": "string",
          "description": "The new fact/entry to add (required for action=add)."
        },
        "old_text": {
          "type": "string",
          "description": "Unique substring of the entry to replace or remove (required for action=replace and action=remove)."
        },
        "new_content": {
          "type": "string",
          "description": "Replacement text (required for action=replace)."
        }
      },
      "required": [
        "action"
      ]
    },
    "toolset": "memory"
  },
  {
    "name": "image_generate",
    "description": "Generate an image from a text prompt using Pollinations AI (free, no key required). Returns a URL to the generated image.",
    "parameters": {
      "type": "object",
      "properties": {
        "prompt": {
          "type": "string",
          "description": "Description of the image to generate."
        },
        "width": {
          "type": "integer",
          "description": "Image width in pixels. Default: 1024."
        },
        "height": {
          "type": "integer",
          "description": "Image height in pixels. Default: 1024."
        }
      },
      "required": [
        "prompt"
      ]
    },
    "toolset": "media"
  },
  {
    "name": "edit_code",
    "description": "Edit a file by replacing one block of text with another. Multi-line strings are supported and encouraged for non-trivial edits. Uses a 9-strategy fuzzy matcher (exact \u2192 whitespace/indent normalised \u2192 unicode-normalised \u2192 block-anchor) so small whitespace drift between your old_string and the file is tolerated. The match must be unique unless replace_all=true. For .sh / .py / .json files the result is syntax-checked and the edit is rejected if it would break the file. Returns a unified diff. Use `patch` for multi-file or multi-hunk changes.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Path to the file to edit (relative to project root)."
        },
        "old_string": {
          "type": "string",
          "description": "Exact text to find. Multi-line is supported. Must match uniquely or pass replace_all=true."
        },
        "new_string": {
          "type": "string",
          "description": "Replacement text. Multi-line is supported."
        },
        "replace_all": {
          "type": "boolean",
          "description": "Replace every occurrence. Default false (must be unique)."
        }
      },
      "required": [
        "path",
        "old_string",
        "new_string"
      ]
    },
    "toolset": "core"
  },
  {
    "name": "patch",
    "description": "Apply a multi-file, multi-hunk patch in V4A format. Use this whenever you need to change several files coherently or make multiple edits in one file. Two-phase: every operation is validated in-memory first; if anything fails NO files are touched. V4A grammar:\n\n*** Begin Patch\n*** Update File: path/to/file\n@@ optional context hint @@\n unchanged context line (space prefix)\n-removed line\n+added line\n*** Add File: path/new\n+content\n*** Delete File: path/old\n*** Move File: src -> dst\n*** End Patch\n\nReturns a combined unified diff on success.",
    "parameters": {
      "type": "object",
      "properties": {
        "patch": {
          "type": "string",
          "description": "Full patch text in V4A format, including the *** Begin Patch / *** End Patch markers."
        }
      },
      "required": [
        "patch"
      ]
    },
    "toolset": "core"
  },
  {
    "name": "repo_map",
    "description": "Get a tree view of the entire project structure.",
    "parameters": {
      "type": "object",
      "properties": {
        "depth": {
          "type": "integer",
          "description": "Max depth for the tree. Defaults to 3."
        }
      },
      "required": []
    },
    "toolset": "inspect"
  },
  {
    "name": "memory_remember",
    "description": "Save an important fact or note to long-term vector memory (LanceDB). Long texts are automatically chunked for precise retrieval. Always include a 'topic' in metadata_json for better search and deduplication. Access timestamps are tracked automatically for pruning.",
    "parameters": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The information to remember."
        },
        "metadata_json": {
          "type": "string",
          "description": "Optional JSON metadata string, e.g. {\"topic\": \"user preferences\"}."
        }
      },
      "required": [
        "text"
      ]
    },
    "toolset": "memory"
  },
  {
    "name": "memory_recall",
    "description": "Search long-term vector memory (LanceDB) using semantic similarity. Returns top matches with topic, access count, and relevance score. Chunks from the same topic are deduplicated automatically. Try 2-3 different phrasings if results are empty.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query to find relevant memories."
        },
        "limit": {
          "type": "integer",
          "description": "Max number of results to return. Defaults to 3."
        }
      },
      "required": [
        "query"
      ]
    },
    "toolset": "memory"
  },
  {
    "name": "sys_info",
    "description": "Get current system stats: CPU load, memory usage, disk space, uptime.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "toolset": "inspect"
  },
  {
    "name": "read_error_log",
    "description": "Read recent errors from the agent error log to diagnose issues.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "toolset": "inspect"
  },
  {
    "name": "custom_tool_manager",
    "description": "Create, list, or delete custom tools. Use 'create' to build new automation scripts.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "create",
            "list",
            "delete"
          ],
          "description": "Action to perform."
        },
        "name": {
          "type": "string",
          "description": "Tool name (used as filename, no spaces)."
        },
        "description": {
          "type": "string",
          "description": "What the tool does."
        },
        "code": {
          "type": "string",
          "description": "The bash script code for the tool."
        },
        "parameters_json": {
          "type": "string",
          "description": "JSON schema of the tool's parameters."
        }
      },
      "required": [
        "action"
      ]
    },
    "toolset": "meta"
  },
  {
    "name": "skill_manager",
    "description": "Manage agent skills. list: show all available skills (core + user). create: make a new skill with a system prompt injected into future turns (optionally enable extra toolsets). bind/unbind: attach a skill to a Telegram topic thread.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "list",
            "create",
            "bind",
            "unbind"
          ],
          "description": "Action to perform."
        },
        "skill": {
          "type": "string",
          "description": "Skill name (required for create/bind/unbind)."
        },
        "prompt": {
          "type": "string",
          "description": "System prompt text injected when this skill is active (required for create)."
        },
        "toolsets": {
          "type": "string",
          "description": "Space-separated extra toolsets to enable for this skill, e.g. 'inspect media'."
        },
        "chat_id": {
          "type": "string",
          "description": "Telegram chat ID (required for bind/unbind)."
        },
        "thread_id": {
          "type": "string",
          "description": "Telegram thread/topic ID (required for bind/unbind)."
        }
      },
      "required": [
        "action"
      ]
    },
    "toolset": "meta"
  },
  {
    "name": "skill_install",
    "description": "Install an external skill from a git repo in one shot \u2014 clones the repo, reads SKILL.md to build the prompt, and writes brain/skills/<name>/. Use this instead of manually cloning + creating files. Requires a public https:// git URL.",
    "parameters": {
      "type": "object",
      "properties": {
        "skill": {
          "type": "string",
          "description": "Short name for the skill (used as directory name and /skill activation key)."
        },
        "repo": {
          "type": "string",
          "description": "Public https:// git URL to clone, e.g. https://github.com/org/repo"
        },
        "toolsets": {
          "type": "string",
          "description": "Optional space-separated extra toolsets to enable, e.g. 'inspect media'."
        }
      },
      "required": [
        "skill",
        "repo"
      ]
    },
    "toolset": "meta"
  },
  {
    "name": "insights",
    "description": "Show token usage statistics and tool usage frequency.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "toolset": "inspect"
  },
  {
    "name": "browser",
    "description": "Control a headless Chromium browser for JS-heavy pages. Use when fetch_url fails or the page requires interaction (login forms, SPAs, infinite scroll). Returns page text and interactive elements.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "navigate",
            "snapshot",
            "click",
            "type",
            "scroll"
          ],
          "description": "navigate: open URL. snapshot: re-read page. click: by CSS selector or visible text. type: fill input. scroll: up or down."
        },
        "url": {
          "type": "string",
          "description": "URL to navigate to (required for navigate)"
        },
        "selector": {
          "type": "string",
          "description": "CSS selector or visible text for click/type/scroll"
        },
        "text": {
          "type": "string",
          "description": "Text to type (required for type action)"
        }
      },
      "required": [
        "action"
      ]
    },
    "toolset": "search"
  },
  {
    "name": "recap",
    "description": "Quickly summarize the current session or a specific session by reading raw logs directly. Use this instead of session_search when you need a summary of what just happened in the current chat.",
    "parameters": {
      "type": "object",
      "properties": {
        "session_id": {
          "type": "string",
          "description": "Specific session ID (e.g. \"tg_670967877\"). Auto-detected if omitted."
        },
        "last_n": {
          "type": "integer",
          "description": "Number of recent messages to include in the recap. Default: 20."
        }
      },
      "required": []
    },
    "toolset": "memory"
  },
  {
    "name": "kanban_show",
    "description": "Read a task's full state and dependencies.",
    "parameters": {
      "type": "object",
      "properties": {
        "task_id": {
          "type": "string",
          "description": "Task ID"
        }
      },
      "required": [
        "task_id"
      ]
    },
    "toolset": "kanban"
  },
  {
    "name": "kanban_create",
    "description": "Create a new kanban task.",
    "parameters": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "Short task title"
        },
        "assignee": {
          "type": "string",
          "description": "Profile to execute this task"
        },
        "body": {
          "type": "string",
          "description": "Full spec / opening post"
        },
        "parents": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Parent task IDs"
        },
        "priority": {
          "type": "integer",
          "description": "Tiebreaker priority"
        }
      },
      "required": [
        "title",
        "assignee"
      ]
    },
    "toolset": "kanban"
  },
  {
    "name": "kanban_complete",
    "description": "Mark your current task done.",
    "parameters": {
      "type": "object",
      "properties": {
        "task_id": {
          "type": "string",
          "description": "Task ID"
        },
        "summary": {
          "type": "string",
          "description": "1-3 sentence handoff summary"
        },
        "metadata": {
          "type": "object",
          "description": "Structured facts/results"
        }
      },
      "required": [
        "task_id",
        "summary"
      ]
    },
    "toolset": "kanban"
  },
  {
    "name": "kanban_block",
    "description": "Stop work and ask for human input.",
    "parameters": {
      "type": "object",
      "properties": {
        "task_id": {
          "type": "string",
          "description": "Task ID"
        },
        "reason": {
          "type": "string",
          "description": "What you need answered"
        }
      },
      "required": [
        "task_id",
        "reason"
      ]
    },
    "toolset": "kanban"
  }
]