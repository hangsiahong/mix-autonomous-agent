[
  {
    "name": "list_files",
    "description": "List files in a directory",
    "parameters": {
      "type": "object",
      "properties": {
        "directory": {
          "type": "string"
        }
      }
    }
  },
  {
    "name": "read_code",
    "description": "Read file content",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "required": ["path"]
    }
  },
  {
    "name": "edit_code",
    "description": "Edit file with search/replace",
    "parameters": {
      "type": "object",
      "properties": {
        "path": { "type": "string" },
        "old_text": { "type": "string" },
        "new_text": { "type": "string" }
      },
      "required": ["path", "old_text", "new_text"]
    }
  },
  {
    "name": "memory_recall",
    "description": "Search long-term memory for relevant past conversations or knowledge",
    "parameters": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "description": "Search query" },
        "limit": { "type": "integer", "description": "Number of memories to return" }
      },
      "required": ["query"]
    }
  },
  {
    "name": "memory_remember",
    "description": "Save an important fact, pattern, or lesson to long-term memory",
    "parameters": {
      "type": "object",
      "properties": {
        "text": { "type": "string", "description": "The fact or insight to remember" },
        "metadata_json": { "type": "string", "description": "Optional JSON metadata" }
      },
      "required": ["text"]
    }
  },
  {
    "name": "custom_tool_manager",
    "description": "Create or manage custom tools for the agent to use",
    "parameters": {
      "type": "object",
      "properties": {
        "action": { "type": "string", "enum": ["create", "list", "delete"] },
        "name": { "type": "string", "description": "Name of the tool (e.g., search_web)" },
        "description": { "type": "string", "description": "What the tool does" },
        "code": { "type": "string", "description": "Full Bash code for the tool script" },
        "parameters_json": { "type": "object", "description": "JSON schema for the tool parameters" }
      },
      "required": ["action"]
    }
  },
  {
    "name": "access_control",
    "description": "Manage bot access whitelist and home chat setting.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": { "type": "string", "enum": ["whitelist", "sethome"], "description": "Action to perform" },
        "target_id": { "type": "string", "description": "The Telegram ID (user, group, or channel) to whitelist or set as home" }
      },
      "required": ["action", "target_id"]
    }
  },
  {
    "name": "web_search",
    "description": "Search the web for current information.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "description": "The search query" }
      },
      "required": ["query"]
    }
  },
  {
    "name": "fetch_url",
    "description": "Read the content of a specific webpage and return it as Markdown.",
    "parameters": {
      "type": "object",
      "properties": {
        "url": { "type": "string", "description": "The full URL to fetch" }
      },
      "required": ["url"]
    }
  },
  {
    "name": "repo_map",
    "description": "Get a recursive tree view of the project structure.",
    "parameters": {
      "type": "object",
      "properties": {
        "depth": { "type": "integer", "description": "Max depth for recursion (default 3)" }
      }
    }
  },
  {
    "name": "insights",
    "description": "Show usage statistics, token consumption, and top tools used by AMA.",
    "parameters": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "sys_info",
    "description": "Get current system status (CPU, RAM, Disk, Time). Use this if you need to know the environment state.",
    "parameters": {
      "type": "object",
      "properties": {}
    }
  }
]
